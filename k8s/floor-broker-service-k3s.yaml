apiVersion: v1
kind: Service
metadata:
  name: floor-broker
  namespace: alpaca-options-trader
spec:
  type: ClusterIP
  selector:
    app: floor-broker
  ports:
    - name: http
      port: 8000
      targetPort: 8000
